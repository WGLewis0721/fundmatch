# Cloudflare deployment note

This branch prepares the authenticated FundMatch application for direct Cloudflare Workers deployment from GitHub Actions. No service-role or migration credentials are included in the deployment workflow.
