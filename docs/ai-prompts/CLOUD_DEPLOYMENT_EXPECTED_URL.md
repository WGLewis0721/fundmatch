# Expected Cloudflare Worker URL

Worker name is `fundmatch`. On the user's existing Cloudflare Workers account, the expected workers.dev origin is typically `https://fundmatch.william-glewis17.workers.dev` once the first deployment succeeds. Treat the actual URL returned by Wrangler as authoritative.
